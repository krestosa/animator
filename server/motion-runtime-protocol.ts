const POST_PATTERN=/const post=\(type,payload=\{\}\\)=>parent\.postMessage\(\{source:(SOURCE|OUT),type,\.\.\.payload\},'\*'\);/;

export function withMotionIrProtocol(source:string):string{
  return source.replace(POST_PATTERN,(_match:string,sourceVariable:string)=>`const __animatorMotionTrack=animation=>{const sourceKind=animation.type==='web-animation'?'waapi':animation.type||'unknown',rawFrames=Array.isArray(animation.keyframes)?animation.keyframes:[],keyframes=rawFrames.map(frame=>{const values={};let offset,easing,composite;for(const[key,value]of Object.entries(frame||{})){if(key==='computedOffset')continue;if(key==='offset'){if(typeof value==='number')offset=value;continue;}if(key==='easing'){if(typeof value==='string')easing=value;continue;}if(key==='composite'){if(typeof value==='string')composite=value;continue;}if(value===null||typeof value==='string'||typeof value==='number')values[key]=value;}return{values,...(offset!==undefined?{offset}:{}),...(easing?{easing}:{}),...(composite?{composite}:{})};}),trigger=sourceKind==='scroll-timeline'?{kind:'scroll-progress',source:'scroll-timeline'}:sourceKind==='css-animation'?{kind:'auto'}:sourceKind==='css-transition'?{kind:'interaction'}:{kind:'unknown'};return{id:animation.id,...(animation.name?{name:animation.name}:{}),target:{elementId:animation.elementId},timing:{start:Number(animation.startTime)||0,...(animation.duration!==undefined?{duration:animation.duration}:{}),...(animation.delay!==undefined?{delay:animation.delay}:{}),...(animation.iterations!==undefined?{iterations:animation.iterations}:{}),...(animation.direction?{direction:animation.direction}:{}),...(animation.easing?{easing:animation.easing}:{}),...(animation.fill?{fill:animation.fill}:{})},properties:Array.isArray(animation.properties)?animation.properties:[],keyframes,trigger,source:{kind:sourceKind,confidence:animation.confidence||'runtime-observed'},runtimeState:animation.runtimeState||'running'};};
  const post=(type,payload={})=>{if(type==='ANIMATION'&&payload.animation){type='MOTION_TRACK';payload={track:__animatorMotionTrack(payload.animation)};}parent.postMessage({source:${sourceVariable},type,...payload},'*');};`);
}
